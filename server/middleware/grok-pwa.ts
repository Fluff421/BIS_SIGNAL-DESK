/** PWA install page + manifest middleware for Nitro/Vercel deploys. */
export default defineEventHandler((event) => {
  // Placeholder — full original registers install page + manifest routes.
  return;
});

function defineEventHandler(fn: (event: unknown) => unknown) {
  return fn;
}
